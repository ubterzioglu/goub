export function SiteFooter() {
  return (
    <footer className="site-footer">
      <p>Private-by-default document workspace for compare, summarize, extract, and export.</p>
      <small>Next.js App Router + Supabase + Claude-first architecture scaffold.</small>
    </footer>
  );
}
